description: Generate, update, and maintain production runbooks per project in the
  f-rr-d thoughts folder. Generates from infra configs, deploy scripts, and team contacts.
  Follows canonical template at assets/runbook-template.md.
name: runbook_manager
tools:
- read
- write
- grep
- glob
- bash
version: 1.0.0
