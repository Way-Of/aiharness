description: Capture the current working session and export as structured session
  history in the f-rr-d thoughts folder. Auto-captures git metrics, problems solved,
  files changed, commits, architecture decisions, and next steps. Follows canonical
  template at assets/session-template.md.
name: session_export
tools:
- read
- write
- grep
- glob
- bash
version: 1.0.0
